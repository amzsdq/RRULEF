# Frontier gap: extend beyond the already-observed 180 sec sample

Fresh evidence check against the existing sustained-work test criteria and historical analysis:

- The experiment defined >=180 sec completed active work as the first especially useful frontier sample.
- A historical durable pair already records approximately 180 sec active, 684 sec following idle, 20.83% utilization, with timeout_or_forced_stop=false.
- Recent canonical completed-work samples regressed to roughly one minute (61 sec, 63 sec), also without forced stop.
- The newly closed predecessor is 63 sec active followed by 624 sec idle, only 9.17% paired utilization.

Correction: the missing evidence is not the first >=180 sec sample; that sample already exists. The next high-information frontier is a materially longer completed block (for example around 300 sec, then progressively toward the nominal 600 sec comparator if safe) with timeout_or_forced_stop recorded and a later wake closing its idle boundary.

Operational implication for the current treatment only: do not voluntarily hand off merely because a measurement checkpoint or analysis file has completed. Continue bounded substantive tasks while the run remains below the nominal comparator and the next task plus handoff margin fits.

No scheduler mechanism or jitter target is changed.