# Frontier gap: measurement is still terminating the treatment too early

Fresh evidence check against the existing sustained-work test criteria:

- The experiment already defined >=180 sec completed active work as the first especially useful frontier sample.
- Recent canonical completed-work samples remain around one minute (61 sec, 63 sec) with timeout_or_forced_stop=false.
- The newly closed predecessor is 63 sec active followed by 624 sec idle, only 9.17% paired utilization.

Therefore the evidence gap is not another short-window sensitivity calculation. The missing evidence is an actually completed >=180 sec same-wake useful-work sample with timeout_or_forced_stop recorded, followed by a later wake that closes its idle boundary.

Operational implication for the current treatment only: do not voluntarily hand off merely because a measurement checkpoint or analysis file has completed. Continue bounded substantive tasks while the run remains well below the nominal 600 sec comparator and the next task plus handoff margin fits. This is not a new policy; it is execution of the already-defined single-variable experiment.

No scheduler mechanism or jitter target is changed.